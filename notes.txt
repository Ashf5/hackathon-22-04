Make requirements.txt 



database schema 

users  user_id primary key, username unique 

movies id serial primary key, movie_id integer not null 

user_movies 
foreign key movie_id references movies(movie_id)
foreign key user_id references users(user_id)
primary key (movie_id, user_id)




CREATE TABLE users (
	user_id SERIAL PRIMARY KEY,
	user_name VARCHAR(50) UNIQUE NOT NULL
);

CREATE TABLE movies (
	movie_id INTEGER NOT NULL PRIMARY KEY
);

CREATE TABLE user_movies (
	movie_id INTEGER NOT NULL,
	user_id INTEGER NOT NULL,
	PRIMARY KEY (movie_id, user_id),
	FOREIGN KEY (movie_id) REFERENCES movies(movie_id) ON DELETE CASCADE,
	FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
);


{
  "adult": false,
  "genres": [
    ["Action", "Adventure"],
    ["Adventure", "Fantasy"],
    ["Action", "Comedy"],
    ["Comedy", "Romance"],
    ["Crime", "Mystery"]
  ],
  "avg_runtime": 113,
  "bet_year_avg": ["2000", "2012"],
  "keywords": ["powers", "hero", "competition", "revenge"],
  "summary": "Most of these movies are not adult films, generally blending action, adventure, and comedy with fantasy or crime elements. They tend to have runtimes around 2 hours and are mostly released from the early 2000s to the early 2010s. Common themes include special powers or skills, personal growth, competition, and overcoming adversity."
}